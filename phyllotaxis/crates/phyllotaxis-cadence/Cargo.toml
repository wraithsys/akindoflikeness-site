[package]
name = "phyllotaxis-cadence"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The harmony engine: when chords change, what they become, and how they arrive."

[dependencies]
phyllotaxis-tuning = { path = "../phyllotaxis-tuning" }
